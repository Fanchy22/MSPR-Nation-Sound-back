{% extends 'base.html.twig' %}

{% block title %}Hello FestivalController!{% endblock %}

{% block body %}
    {{ form_start(festivalForm) }}

    {{ form_label(festivalForm.name) }}
    {{ form_widget(festivalForm.name) }}
    {{ form_errors(festivalForm.name) }}<hr>
    <input type="submit" value="Envoyer">
    {{ form_end(festivalForm) }}
{% endblock %}
