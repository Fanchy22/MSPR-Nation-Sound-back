{% extends 'base.html.twig' %}

{% block title %}Saisie des artistes!{% endblock %}

{% block body %}
    {{ form_start(artisteForm) }}

    {{ form_label(artisteForm.name) }}
    {{ form_widget(artisteForm.name) }}
    {{ form_errors(artisteForm.name) }}<hr>

    {{ form_label(artisteForm.genre) }}
    {{ form_widget(artisteForm.genre) }}
    {{ form_errors(artisteForm.genre) }}<hr>

    {{ form_label(artisteForm.thumbnail) }}
    {{ form_widget(artisteForm.thumbnail) }}
    {{ form_errors(artisteForm.thumbnail) }}<hr>

    {{ form_label(artisteForm.day) }}
    {{ form_widget(artisteForm.day) }}
    {{ form_errors(artisteForm.day) }}<hr>

    {{ form_label(artisteForm.time) }}
    {{ form_widget(artisteForm.time) }}
    {{ form_errors(artisteForm.time) }}<hr>

    {{ form_label(artisteForm.timeValue) }}
    {{ form_widget(artisteForm.timeValue) }}
    {{ form_errors(artisteForm.timeValue) }}<hr>

    {{ form_label(artisteForm.place) }}
    {{ form_widget(artisteForm.place) }}
    {{ form_errors(artisteForm.place) }}<hr>

    {{ form_label(artisteForm.type) }}
    {{ form_widget(artisteForm.type) }}
    {{ form_errors(artisteForm.type) }}<hr>

    {{ form_label(artisteForm.description) }}
    {{ form_widget(artisteForm.description) }}
    {{ form_errors(artisteForm.description) }}<hr>

    <input type="submit" value="Envoyer">
    {{ form_end(artisteForm) }}

    {% for article in articles %}
        {{ artiste.name }}
    {% endfor %}

{% endblock %}
